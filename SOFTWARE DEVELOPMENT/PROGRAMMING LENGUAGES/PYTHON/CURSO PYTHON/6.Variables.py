# Variables y costantes en python

# Constantes  y variables en python
# Una variable es una unidad de datos que puede cambiar de valor. El programador puede decidir el nombre de las variables, se recomienda que sea claro y conciso, y también puede escribir funciones que trabajen con los valores contenidos en ellas.

# Para modificar el valor de una variable, simplemente se le asigna un nuevo valor en el programa, este puede ser un literal, una expresión, una llamada a una función o una combinación de todos ellos.

# Un literal es un dato puro, que puede ser un número, una cadena de caracteres o un booleano.

# Ejemplo:

a= 10000 # a se inicializa con un número entero osea un literal
b= a * 2/1000 # b se inicializa con una operación aritmetica osea una expresión
c= "JUAN CARLOS" # c se inicializa con una cadena
print( c, "debe la suma de $", a+b)

# Declarando e inicializando multiples variables a la vez
#Ejemplo
name="Juan David"
surname="Paniagua Cano"
age=21
ocupation="Software Developer"


# Existen unas variables especiales denominadas acumuladores y otras denominadas contadores.

# Un acumulador es una variable a la que le vamos sumando un determinado valor. (Generalmente se usan en los bucles para acumular algun valor por cada iteración)

# Ejemplo: suma=suma+edad

# La variable suma, acumula las edades.

# Un contador es una variable a la cual le vamos sumando/restando un valor constante. (Generalmente se usan en los bucles para ir contando en que vuelta va el bucle o mejor dichos es lo que cuenta las iteraciones del bucle)

# Ejemplo: total=total+1
# Ejemplo2: contador = contador + 1

# La variable total y contador se incrementa en 1 cada vez que se ejecuta esta instrucción.
